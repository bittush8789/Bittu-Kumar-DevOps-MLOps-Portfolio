apiVersion: v1
kind: Service
metadata:
  name: bittu-portfolio-service
  namespace: default
  labels:
    app: bittu-portfolio
spec:
  selector:
    app: bittu-portfolio
  type: LoadBalancer
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
